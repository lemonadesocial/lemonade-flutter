import 'package:flutter/material.dart';

class CommunitySquareTile extends StatelessWidget {
  const CommunitySquareTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
