enum TokensListingType {
  onSale,
  created,
  collected,
  sold,
}

enum OrderKind {
  AUCTION,
  DIRECT,
}

enum OrderSortBy {
  bidAmount,
  bidder,
  contract,
  createdAt,
  currency,
  id,
  kind,
  maker,
  network,
  open,
  openFrom,
  openTo,
  orderId,
  paidAmount,
  price,
  taker,
  updatedAt,
}
