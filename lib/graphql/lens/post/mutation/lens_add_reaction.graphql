mutation LensAddReaction($request: AddReactionRequest!) {
  addReaction(request: $request) {
    ... on AddReactionResponse {
      success
    }
    ... on AddReactionFailure {
      reason
    }
  }
}
