mutation CreateCastReaction(
  $type: ReactionType!
  $targetCastId: ParentCastInput
  $targetUrl: String
) {
  createCastReaction(
    type: $type
    target_cast_id: $targetCastId
    target_url: $targetUrl
  )
}
