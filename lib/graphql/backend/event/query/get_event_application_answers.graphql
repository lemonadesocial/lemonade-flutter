query GetEventApplicationAnswers(
  $event: MongoID!
  $user: MongoID
  $email: String
) {
  getEventApplicationAnswers(event: $event, user: $user, email: $email) {
    _id
    answer
    question
    question_expanded {
      question
      _id
      required
    }
    user
  }
}