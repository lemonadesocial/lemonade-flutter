query Answers($event: MongoID!, $user: MongoID) {
  getEventApplicationAnswers(event: $event, user: $user) {
    _id
    answer
    question
    question_expanded {
      _id
      question
    }
    user
  }
}